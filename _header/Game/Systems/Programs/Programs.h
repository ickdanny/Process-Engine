#pragma once

/*
#include "PlayerPrograms.h"
#include "PickupPrograms.h"
#include "EnemyPrograms.h"
 */

namespace process::game::systems {

	struct Programs {
		/*
	private:
		//fields
		resources::BitmapStorage* bitmapStoragePointer{};

	public:
		const PlayerPrograms playerPrograms;	//uninitialized!
		const PickupPrograms pickupPrograms;	//uninitialized!
		const EnemyPrograms enemyPrograms;	//uninitialized!

		Programs(
			resources::BitmapStorage* bitmapStoragePointer
		)
			: playerPrograms{ bitmapStoragePointer }
			, pickupPrograms{ bitmapStoragePointer }
			, enemyPrograms{ bitmapStoragePointer, &pickupPrograms } 
		{
		}
		 */
	};
}