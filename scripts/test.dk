func velConsumer(velocity){
    print(velocity);
}

let velocity = makePolar(2, 90);
print("Testing column");
column(velocity, 5, 0.5, velConsumer);
print("Testing ring");
ring(velocity, 30, velConsumer);
print("Testing arc");
arc(velocity, 10, 5, velConsumer);