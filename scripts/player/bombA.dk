let speed = 2.5;
let symmetry = 8;

let pos = entityPosition();

func velConsumer(velocity){
    spawn("scythe", pos, velocity, "scytheSpiral");
}

let angle = random(0.0, 360.0);
let baseVel = makePolar(speed, angle);

#velConsumer(baseVel);
ring(baseVel, symmetry, velConsumer);
