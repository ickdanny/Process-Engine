# func turningBullet(directionBool)

let angleShift = diff(30.0, 60.0, 90.0, 120.0);
let turnTime = 70;

if(directionBool){
    angleShift = -angleShift;
}

shiftAnglePeriod(entityAngle() + angleShift, turnTime);