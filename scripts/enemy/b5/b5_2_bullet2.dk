let waitTime = 40;
let accelTime = 40;
let roughFinalSpeed = diff(0.7, 0.8, 0.9, 1.0);
let accelPolar = makePolar(roughFinalSpeed / accelTime, randomAngle());

timer(waitTime);

for(let t = 0; t < accelTime; t = t + 1){
    setVelocity(entityVelocity() + accelPolar);
    stall();
}