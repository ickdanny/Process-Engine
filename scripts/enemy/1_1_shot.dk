let speed = 1.9;
let angle = entityAngle();

let arcSymmetry = diff(1, 3, 5, 7);
let arcIncrement = diff(0.0, 22.0, 19.0, 16.0);

let position = entityPosition();
func spawnBullet(velocity){
    spawn("mediumLRed", position, velocity, "1_1_bullet");
}

arc(makePolar(speed, angle), arcSymmetry, arcIncrement, spawnBullet);