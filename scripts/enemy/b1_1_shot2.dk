let symmetry = diff(4, 5, 6, 7);
let initSpeed = 1.8;
let angle = random(0.0, 360.0);

let baseVelocity = makePolar(initSpeed, angle);

let pos = entityPosition();
func spawnBullet(velocity){
    spawn("mediumBlue", pos, velocity, "b1_1_shot2_bullet1");
}

ring(baseVelocity, symmetry, spawnBullet);