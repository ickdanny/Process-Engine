let initWaitTime = 5;
let turnTime = 40;
let angleOffset = 80.0;
let finalSpeed = 2.5;

timer(initWaitTime);

if(getX(toVector(entityVelocity())) > 0.0){
    angleOffset = -angleOffset;
}
shiftVelocityTurnPeriod(finalSpeed, entityAngle() + angleOffset, turnTime);