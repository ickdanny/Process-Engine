let lifetime = random(45, 55);
let slowestSpeed = 0.25;
let angleShift = -90.0;

removeOutbound();
shiftVelocityTurnPeriod(slowestSpeed, entityAngle() + angleShift, lifetime);
addDeathSpawn("b1_1_shot1_bullet2Explode");
die();