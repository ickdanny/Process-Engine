let initSpeed = 1.5;
let initVelocity = makePolar(initSpeed, -90.0);

func spawnEnemy(position, velocity){
    spawn("automatonBlue", position, velocity, "3_5_enemy");
}

spawnEnemy(getTopSpawnLeft(gameWidth / 2.0), initVelocity);