let health = 600;
let slowTime = 54;
let waitTime = 10;
let shootSpeed = 0.19;
let shootSpeedTime = 35;
let finalWaitTime = 20;
let finalSpeed = 0.5;
let finalAngle = 90.0;
let finalSpeedTime = 81;

setHealth(health);
addDeathSpawn("largePower");

shiftSpeedPeriod(0.0, slowTime);
addSpawn("3_6_shot");

timer(waitTime);
shiftSpeedPeriod(shootSpeed, shootSpeedTime);

stallUntil(isNotSpawning);
timer(finalWaitTime);
shiftVelocityTurnPeriod(finalSpeed, finalAngle, finalSpeedTime);