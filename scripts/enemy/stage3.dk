timer(630);
addSpawn("3_1_spawner"); # wave 1  =

timer(1200);
#spawn("boss3", getTopSpawnLeft(gameWidth / 2.0), zeroPolar, "b3_master");

stallUntil(isWin);
timer(timeBeforePostDialogue);

showDialogue("3_2");
stallUntil(isDialogueOver);
endStage();