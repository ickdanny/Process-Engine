let symmetry = diff(18, 24, 30, 36);
let speed = 0.5;

let position = entityPosition();
func spawnBullet(velocity){
    spawn("largeLRed", position, velocity);
}

ring(makePolar(speed, randomAngle()), symmetry, spawnBullet);