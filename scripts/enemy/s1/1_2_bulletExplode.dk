let symmetry = diff(4, 8, 12, 16);
let angle = randomAngle();
let speed = 1.9;

func spawnBullet(velocity){
    spawn("smallLRed", entityPosition(), velocity);
}

ring(makePolar(speed, angle), symmetry, spawnBullet);