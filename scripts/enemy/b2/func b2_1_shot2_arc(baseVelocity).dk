# func b2_1_shot2_arc(baseVelocity);

let symmetry = diff(4, 5, 6, 7);
let increment = diff(50.0, 60.0, 65.0, 70.0) / symmetry;

let position = entityPosition();
func spawnBullet(velocity){
    spawn("mediumLRed", position, velocity);
}

arc(baseVelocity, symmetry, increment, spawnBullet);