let symmetry = 5;
let speed = 2.3 + 0.4 * getDifficulty();
let baseAngle = -90.0;

let position = entityPosition();
func spawnBullet(velocity){
    spawn("sharpClear", position, velocity, "b2_4_bullet1");
}

ring(makePolar(speed, baseAngle), symmetry, spawnBullet);
