let health = 3000;
let timeBeforeMove = 93;
let timeAfterMove = 79;
let rLow = 35;
let rHigh = 55;
let maxSpeed = 0.5;

setHealth(health);

while(true){
    addSpawn("b2_4_shot1");
    timer(timeBeforeMove);
    addSpawn("b2_4_shot2");
    boundGotoDecelerating(bossXLow, bossXHigh, bossYLow, bossYHigh - 15.0, rLow, rHigh, maxSpeed);
    timer(timeAfterMove);
}