if(getDifficulty() >= 2){
    let speed = 1.9;
    let symmetry = 8;

    let position = entityPosition();
    func spawnBullet(velocity){
        spawn("mediumClear", position, velocity);
    }

    ring(makePolar(speed, 0.0), 8, spawnBullet);
}